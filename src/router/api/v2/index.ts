import { Router } from 'express';

// v2 router — add new or updated domain routers here as v2 endpoints are introduced.
export const v2Router = Router();

// v2Router.use('/examples', exampleV2Router);
